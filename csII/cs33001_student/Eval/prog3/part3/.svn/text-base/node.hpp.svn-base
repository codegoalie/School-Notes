/* Node Implementation
 * Chris Marshall
 * 4.4.2010
 */

#ifndef _NODE_HPP_
#define _NODE_HPP_

template <typename T>
class Node
{
  public:
    T data;
    Node<T> *next;

    Node() { next = 0; }; // default next to 0
    Node(T d) { data = d; next = 0; }; // use data given for data
};

#endif
